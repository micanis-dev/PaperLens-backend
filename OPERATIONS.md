# PaperLens API 運用ランブック

この文書は、PaperLens API、PostgreSQL、クレジット台帳、Stripe Webhookを本番で運用するための最小手順です。秘密値やPDF本文はコマンドの引数・ログ・チケットへ貼り付けません。

## デプロイ前チェック

- ステージングで同じ料金表バージョンのFree / Plus / Pro / Ultraを検証する。
- Checkout成功後、Webhook処理前はFree相当のままであることを確認する。
- 有料プランのダウングレードはSubscription Schedule、Freeへの変更は`cancel_at_period_end`がStripe側に登録され、次回更新まで現プランが維持されることを確認する。既存の予約を上位プラン変更で解除できることも確認する。
- 正常系、署名不正、期限切れ、重複、順序逆転、支払い失敗、7日猶予終了を検証する。
- 予約・確定・返却・返金・期限切れを台帳から再計算し、残高の直接上書きがないことを確認する。
- 料金表のp50・p95・上限利用量で原価が月額を超えないことを確認する。
- 料金換算を変更する場合は、原価（モデル別micro-USD/百万トークン）、換算単位、出力重み、`version`を更新し、`/v1/admin/rates`の監査記録と台帳の新旧バージョンを確認する。
- `FRONTEND_ORIGINS`、`FRONTEND_BASE_URL`、Stripe URL、Cookie設定が本番値であることを確認する。
- アカウント削除を再認証済みセッションで予約し、24時間以内の取消と期限到達後の匿名化・関連行削除をステージングで確認する。

## バックアップと復元

Supabase FreeのPostgreSQLを利用する。Freeプランでは自動バックアップやPoint-in-Time Recoveryを前提にできないため、重要データを扱う段階ではバックアップ方式と有料プランへの移行を先に決める。保持先は本番DBとは別のアクセス境界に置き、復元テストを月次で行う。バックアップにはPDF本体・論文本文・BYOKキーは含まれない。

1. デプロイ前にバックアップの成功時刻と復元可能性を確認する。
2. 復元時は新しい一時DBへ復元し、`schema_migrations`、`subscriptions`、`credit_ledger`、`translation_requests`、`webhook_events`の件数と制約を確認する。
3. 台帳の残高を `grant + refund - consume - expire - reserve + consume + release` として再計算し、負の残高や未完了予約を抽出する。
4. 検証後にのみ接続先を切り替える。元DBは確認完了まで変更せず保持する。

ローカルPDFや注釈はブラウザのIndexedDBにあり、サーバーDBの復元では戻りません。ユーザーにはアプリのZIPバックアップを別途案内します。

## Stripe Webhook障害

- `/v1/stripe/webhook` はStripe署名が必要で、画面セッションCookieは要求しません。
- 同じイベントを再送しても一度だけ状態を適用します。処理中にプロセスが停止した場合は短い処理予約の期限後に再試行可能です。
- Stripeダッシュボードから再送する前に、`webhook_events`のevent ID、subscription ID、customer ID、処理時刻だけを監査します。決済情報や本文はログへ出しません。
- 順序逆転イベントはイベント時刻の比較で古い状態を適用しません。
- 署名不正・対象顧客不一致・未知のJSONは400として扱い、アプリケーションログには本文を出力しません。

## アカウント削除

- `POST /v1/account/deletion` はOAuthまたはマジックリンク発行から15分以内のセッションだけを受け付け、予約時刻から24時間は取消可能です。
- 期限到達時は、APIの認証済みリクエストまたは定期的な削除ジョブから `ProcessDue` を実行します。PostgreSQLでは契約、クレジット台帳、翻訳リクエスト、認証チャレンジを同一トランザクションで削除し、users行を匿名化した削除済み状態にします。
- StripeのWebhook監査用イベントIDと、法令上必要な決済事業者側の記録は削除対象外です。削除済みユーザーの既存セッションはAPIで認証不可になります。

## 障害時の切り分け

1. `/v1/healthz`でプロセスとDB接続を確認する。ヘルスチェックは実翻訳を実行しない。
2. 管理者セッションで `/v1/internal/metrics` を取得し、5xx率、429率、翻訳失敗率、予約残高不整合、Providerレイテンシ、月次原価を確認する。出力にはユーザーID、本文、秘密値を含めない。
3. 管理LLMだけを停止する場合も、ローカルライブラリ・BYOK・ローカルLLMの経路は継続させる。
4. Provider障害では自動的にBYOKへ切り替えない。送信先変更はユーザーの明示操作とする。
5. 台帳不整合が疑われる場合は新規の管理LLM受付を停止し、台帳を修正する管理者操作は`refund`など追記型取引だけで行う。

メトリクスの `paperlens_managed_admission_open` が `1` の場合、管理LLMの新規受付は自動停止しています。直近5分以内の台帳不整合、または5件以上の翻訳結果で失敗率が50%以上になると開き、5分後に自動的に再評価します。BYOK・ローカルLLMはこの遮断の影響を受けません。

## ロールバック

アプリ、DBマイグレーション、料金表は別々にロールバックする。後方互換なカラム追加を先に行い、旧アプリが読めることを確認してから新アプリを段階展開する。破壊的変更を含む場合は、切り戻し用の読み書き互換期間と復元手順を先に決める。
